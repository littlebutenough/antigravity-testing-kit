# ============================================================
# STEP 6: REVIEW & OPTIMIZATION (Review & Refactoring)
# ============================================================
# Workflow: /generate_automation_from_testcases (continued)
# Skill:    qa_automation_engineer
# ============================================================
# USAGE:
# 1. Send this prompt AFTER test has PASSED in Step 5
# 2. AI will review and cleanup code
# 3. Receive final code → merge into repository
# ============================================================

---START---

/generate_automation_from_testcases

Test has **PASSED** on local machine. Before merging into repository, please review and optimize code.

## Refactoring Tasks:

### 1. Delete debug artifacts

- Delete all `console.log`, `System.out.println`, `print()` generated during debugging
- Delete commented code (`//`, `/* */`) no longer used
- Delete variables/locators declared but not used (unused code)

### 2. Check code quality

- [ ] Do variable and method names have clear meaning?
- [ ] Are there any methods too long that need splitting? (>20 lines)
- [ ] Is there duplicate logic that needs extraction into helper?
- [ ] Are import statements clean?

### 3. Check test stability

- [ ] Did any hard sleep slip through?
- [ ] Are assertions sufficient and accurate?
- [ ] Is test data unique each run?
- [ ] Is each test case independent? (does not depend on other tests)

### 4. CI/CD Readiness

[Customize according to your framework:]

Playwright:
```typescript
// Attach tags for selective execution
test.describe('Login Module', () => {
  test.describe.configure({ mode: 'parallel' });
  // ...
});
```

Selenium/TestNG:
```java
@Test(groups = {"smoke", "regression"})
public void testLoginSuccess() { ... }
```

Appium:
```java
@Test(groups = {"mobile", "regression"})
public void testLoginMobile() { ... }
```

### 5. Documentation (optional)

- Add descriptive comments for complex methods
- Update README if there are project structure changes

---

Please return the **final most complete version** of code (Clean Code), ready to commit.
