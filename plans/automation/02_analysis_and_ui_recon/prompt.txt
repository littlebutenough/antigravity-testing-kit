# ============================================================
# STEP 2: ANALYSIS & UI INVESTIGATION (Analysis & UI Recon)
# ============================================================
# Workflow: /generate_automation_from_testcases (continued)
# Skill:    qa_automation_engineer + ui_debug_agent
# ============================================================
# USAGE:
# 1. Send this prompt AFTER AI has confirmed in Step 1
# 2. Fill in URL, test account, and Manual Test Cases
# 3. AI will AUTOMATICALLY open browser, inspect DOM, collect locators
# 4. Review locator table returned by AI → proceed to Step 3
# ============================================================

---START---

Below are the **Manual Test Cases** to be automated. Before writing code, you **MUST** investigate and obtain real locators from the UI (UI Recon).

## Environment Information

- **URL:** [Enter URL. Example: https://dev.crm.system.local/login]
- **Test Account:** [Example: user_admin@domain.com / password123]
- **Browser:** [Chrome / Firefox / Edge — default Chromium]

## Manual Test Cases

[Copy/Paste Manual Test Cases here. Example:

**TC_01: Successful Login**
1. Open Login page
2. Enter valid email: admin@crm.com
3. Enter valid password: Admin@123
4. Click "Login" button
5. Verify: Redirect to Dashboard, display user name

**TC_02: Login Failed — Wrong Password**
1. Open Login page
2. Enter email: admin@crm.com
3. Enter wrong password: WrongPass
4. Click "Login" button
5. Verify: Display error message "Wrong password"]

---

## Task

Use **browser tool** (Playwright MCP / Selenium MCP) to:

1. **Open browser** with provided URL (viewport 1920x1080)
2. **Navigate** according to each step in Test Cases
3. **Collect** all necessary locators by:
   - Reading Accessibility Tree
   - Inspecting DOM attributes: `id`, `data-testid`, `name`, `aria-label`, `role`, `placeholder`
   - ABSOLUTELY **do not guess** selectors — only take from real DOM
4. **Prioritize** locator according to order (refer to `.agent/rules/locator_strategy.md`):
   - Playwright: `getByRole` > `getByLabel` > `getByPlaceholder` > `getByTestId` > CSS > XPath
   - Selenium: `id` > `data-testid` > `name` > `cssSelector` > `xpath`
5. **Return result** as a table:

| Page | Element | Locator Type | Locator Value | Fallback Locator |
|------|---------|-------------|---------------|------------------|

⏸️ **Pause** after completing the locator table for me to review before proceeding to Step 3.
